#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: int b parameter
 * @b: int a parameter
 * Return: sum of a and b
 */
int add(int a, int b)
{
	int result = a + b;
return (result);
}
